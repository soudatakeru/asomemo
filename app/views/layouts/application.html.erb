<!DOCTYPE html>
<html>
  <head>
    <script src="index.js"></script>
    <div class="messege">
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class: key) %>
        <% end %>
    </div>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Asomemo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.5.1.min.js">
    <script src="./js/script.js"></script>
    <header class="header">
      <div class="inner">
        <div class="nav">
          
          <% if user_signed_in? %>
            <div class="nav__left"> 
              <%= link_to image_tag("logo9.png", class: :logo_sub), favorites_path(current_user) %>
              <%= link_to image_tag("logo8.png", class: :logo_sub), user_path(current_user.id), class: :greeting__link%> 
            </div> 
          <% else %>
            <div class="nav__left"> 
              <%= link_to image_tag("logo9.png", class: :logo_sub), root_path %>
              <%= link_to image_tag("logo8.png", class: :logo_sub), root_path %>
            </div> 
          <% end %>
          <div class="nav__main">
            <%= link_to image_tag("logo.png", class: :logo_main), root_path %>
          </div>
          <% if user_signed_in? %>
             <div class="nav__right"> 
               <%= link_to image_tag("logo6.png", class: :logo_sub), destroy_user_session_path, method:  :delete %>
               <%= link_to image_tag("logo7.png", class: :logo_sub), new_event_path %>
             </div> 
          <% else %>
            <div class="nav__right">
              <%= link_to image_tag("logo3.png", class: :logo_sub), new_user_session_path %>
              <%= link_to image_tag("logo5.png", class: :logo_sub), new_user_registration_path %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright © PROTO SPACE All rights reserved.</p>
    </footer>
  </body>
</html>